apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accommodatio-app.fullname" . }}-test-connection"
  labels:
    {{- include "accommodatio-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "accommodatio-app.fullname" . }}:{{ .Values.apiGateway.containerPort }}']
  restartPolicy: Never
